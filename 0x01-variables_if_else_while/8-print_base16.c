#include <stdio.h>
/**
 * main - Entry point
 *
 * print nums in base 16
 * Return: always 0
 */
int main(void)
{
	char x = 'ox0';

	while (x < f)
	{
		putchar(x);
		x++;
	}
	putchar('\n');
	return (0);
}
