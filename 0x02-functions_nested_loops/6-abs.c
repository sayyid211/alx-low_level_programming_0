#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 */

int _abs(int)
{
	int retabs(int r);
	retabs();
}
int retabs(int r)
{
	int r;
	
	if (r < 0)
	{
		return (r / -1);
	}
	else
	{
		return (r);
	}
}
