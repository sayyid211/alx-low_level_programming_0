#include <stdio.h>
#include "main.h"
/**
 * main - entry point
 * @argc: arguments count
 * @argv: arguments vector
 * Return: 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
